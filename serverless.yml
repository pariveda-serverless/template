# TODO: Update Serverless service name
# NOTE: Service name must match the repository name, but replace all '.' with '-' (this is a restriction of Cloudformation naming conventions)
service: gs-svc-template

# DO NOT MODIFY!!!
provider:
  name: aws
  runtime: nodejs4.3
  stage: ${opt:stage}
  # NOTE: The lambda role from the security stack is passed in via the serverless deploy command
  role: ${opt:lambdaRoleARN}

functions:
  hello:
    handler: index.hello
    events:
      - http:
          path: hello/world
          method: get

